apiVersion: v1
kind: Service
metadata:
  name: harbor-valkey
  labels:
    app.kubernetes.io/name: harbor
    app.kubernetes.io/component: cache
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: harbor
    app.kubernetes.io/component: cache
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
