# Minimal profile for small VMs (2 CPU / 4 GB RAM).

cassandra:
  jvm:
    maxHeapSize: 512M
    newHeapSize: 128M
  resources:
    requests:
      cpu: 300m
      memory: 512Mi
    limits:
      cpu: "1"
      memory: 1Gi
  persistence:
    size: 10Gi
