apiVersion: v1
kind: Service
metadata:
  name: nextcloud-http
  labels:
    app.kubernetes.io/name: nextcloud
    app.kubernetes.io/component: app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nextcloud
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
