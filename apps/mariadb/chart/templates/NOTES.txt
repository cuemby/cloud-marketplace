Cuemby Cloud Marketplace — MariaDB deployed!

Connect to MariaDB:

  {{- if eq .Values.mariadb.primary.service.type "NodePort" }}
  mysql -h <VM_IP> -P {{ .Values.mariadb.primary.service.nodePorts.mysql }} -u root -p
  {{- else }}
  kubectl get svc -n {{ .Release.Namespace }}
  {{- end }}

Check deployment status:
  cat /var/lib/cuemby/marketplace-state.json
