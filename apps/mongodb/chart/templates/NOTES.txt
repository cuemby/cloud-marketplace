Cuemby Cloud Marketplace — MongoDB deployed!

Connect to MongoDB:

  {{- if eq .Values.mongodb.service.type "NodePort" }}
  mongosh --host <VM_IP> --port {{ .Values.mongodb.service.nodePorts.mongodb }} -u root -p
  {{- else }}
  kubectl get svc -n {{ .Release.Namespace }}
  {{- end }}

Check deployment status:
  cat /var/lib/cuemby/marketplace-state.json
