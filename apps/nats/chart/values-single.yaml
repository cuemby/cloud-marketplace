# Minimal profile for small VMs (1 CPU / 1 GB RAM).
# Layered on top of values.yaml with lower resource limits.

nats:
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 250m
      memory: 256Mi
