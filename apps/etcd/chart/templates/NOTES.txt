Cuemby Cloud Marketplace — etcd deployed!

Connect to etcd:

  {{- if eq .Values.etcd.service.type "NodePort" }}
  etcdctl --endpoints=http://<VM_IP>:{{ .Values.etcd.service.nodePorts.client }} --user=root:<password> endpoint health
  {{- else }}
  kubectl get svc -n {{ .Release.Namespace }}
  {{- end }}

Check deployment status:
  cat /var/lib/cuemby/marketplace-state.json
