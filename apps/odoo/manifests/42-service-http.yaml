apiVersion: v1
kind: Service
metadata:
  name: odoo-web
  labels:
    app.kubernetes.io/name: odoo
    app.kubernetes.io/component: app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: odoo
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: 80
      targetPort: 8069
      protocol: TCP
