Cuemby Cloud Marketplace — Jenkins deployed!

Access Jenkins:

  https://{{ .Release.Name }}.sslip.io

  Note: Jenkins may take 2-3 minutes to start due to JVM initialization.

Check deployment status:
  cat /var/lib/cuemby/marketplace-state.json
