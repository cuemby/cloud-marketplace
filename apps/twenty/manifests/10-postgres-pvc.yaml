apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: twenty-postgres-data
  labels:
    app.kubernetes.io/name: twenty
    app.kubernetes.io/component: database
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: ${PARAM_TWENTY_DB_DATA_SIZE}
