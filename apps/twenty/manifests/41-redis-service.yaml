apiVersion: v1
kind: Service
metadata:
  name: twenty-redis
  labels:
    app.kubernetes.io/name: twenty
    app.kubernetes.io/component: cache
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: twenty
    app.kubernetes.io/component: cache
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
