Cuemby Cloud Marketplace — OpenSearch deployed!

Connect to OpenSearch:

  curl -u admin:<password> http://<VM_IP>:30921

Check cluster health:
  curl -u admin:<password> http://<VM_IP>:30921/_cluster/health?pretty

Check deployment status:
  cat /var/lib/cuemby/marketplace-state.json
