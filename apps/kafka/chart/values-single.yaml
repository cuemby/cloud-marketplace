# Minimal profile for small VMs (2 CPU / 4 GB RAM).

kafka:
  controller:
    resources:
      requests:
        cpu: 200m
        memory: 256Mi
      limits:
        cpu: 500m
        memory: 1Gi
    persistence:
      size: 8Gi
