Cuemby Cloud Marketplace — Kafka deployed!

Connect to Kafka:

  Bootstrap server: <VM_IP>:30092

  Produce:
    kafka-console-producer.sh --broker-list <VM_IP>:30092 --topic test

  Consume:
    kafka-console-consumer.sh --bootstrap-server <VM_IP>:30092 --topic test --from-beginning

Check deployment status:
  cat /var/lib/cuemby/marketplace-state.json
