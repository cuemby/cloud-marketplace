#cloud-config
# Cloud-init user-data for deploying NATS JetStream via Cuemby Cloud Marketplace.
#
# Auth token is auto-generated if not provided.
#
# VM requirements: 2 CPU, 4 GB RAM, 20 GB disk, Ubuntu 22.04+ recommended.
# Firewall: allow inbound TCP on ports 30422 (NATS client) and 30822 (Monitoring).

package_update: true

packages:
  - curl
  - git
  - jq

write_files:
  - path: /opt/cuemby/marketplace-env.sh
    permissions: "0600"
    content: |
      #!/usr/bin/env bash
      export APP_NAME="nats-jetstream"
      export APP_VERSION="{{app-version}}"
      # Credentials (Cuemby Cloud interpolates {{...}}; auto-generated otherwise)
      export PARAM_NATS_AUTH_TOKEN="{{param-nats-auth-token}}"
      # Optional parameters (defaults applied in pre-install hook)
      export PARAM_NATS_DATA_SIZE="{{param-nats-data-size}}"

runcmd:
  - git clone https://github.com/cuemby/cloud-marketplace.git /opt/cuemby/marketplace
  - [bash, -c, "source /opt/cuemby/marketplace-env.sh && /opt/cuemby/marketplace/bootstrap/entrypoint.sh 2>&1 | tee /var/log/cuemby/bootstrap.log"]
