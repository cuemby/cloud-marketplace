apiVersion: v1
kind: Service
metadata:
  name: devtron-dashboard-http
  labels:
    app.kubernetes.io/name: devtron
    app.kubernetes.io/component: dashboard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: devtron
    app.kubernetes.io/component: dashboard
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
