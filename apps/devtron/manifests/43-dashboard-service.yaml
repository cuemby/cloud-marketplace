apiVersion: v1
kind: Service
metadata:
  name: devtron-dashboard
  labels:
    app.kubernetes.io/name: devtron
    app.kubernetes.io/component: dashboard
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: devtron
    app.kubernetes.io/component: dashboard
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
