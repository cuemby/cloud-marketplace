Cuemby Cloud Marketplace — RabbitMQ deployed!

Connect to RabbitMQ:

  {{- if eq .Values.rabbitmq.service.type "NodePort" }}
  AMQP:       amqp://<user>:<password>@<VM_IP>:{{ .Values.rabbitmq.service.nodePorts.amqp }}
  Management: http://<VM_IP>:{{ .Values.rabbitmq.service.nodePorts.manager }}
  {{- else }}
  kubectl get svc -n {{ .Release.Namespace }}
  {{- end }}

Check deployment status:
  cat /var/lib/cuemby/marketplace-state.json
