Cuemby Cloud Marketplace — PostgreSQL deployed!

Connect to PostgreSQL:

  {{- if eq .Values.postgresql.primary.service.type "NodePort" }}
  psql -h <VM_IP> -p {{ .Values.postgresql.primary.service.nodePorts.postgresql }} -U postgres
  {{- else }}
  kubectl get svc -n {{ .Release.Namespace }}
  {{- end }}

Check deployment status:
  cat /var/lib/cuemby/marketplace-state.json
