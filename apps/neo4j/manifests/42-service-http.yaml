apiVersion: v1
kind: Service
metadata:
  name: neo4j-http
  labels:
    app.kubernetes.io/name: neo4j
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: neo4j
    app.kubernetes.io/component: database
  ports:
    - name: http
      port: 80
      targetPort: 7474
      protocol: TCP
