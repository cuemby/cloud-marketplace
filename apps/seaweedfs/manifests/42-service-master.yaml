apiVersion: v1
kind: Service
metadata:
  name: seaweedfs-master
  labels:
    app.kubernetes.io/name: seaweedfs
    app.kubernetes.io/component: storage
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: seaweedfs
    app.kubernetes.io/component: storage
  ports:
    - port: 9333
      targetPort: 9333
      nodePort: ${PARAM_SEAWEEDFS_MASTER_NODEPORT}
      protocol: TCP
