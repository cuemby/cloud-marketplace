apiVersion: v1
kind: Service
metadata:
  name: joomla-mariadb
  labels:
    app.kubernetes.io/name: joomla
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: joomla
    app.kubernetes.io/component: database
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
