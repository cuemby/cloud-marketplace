apiVersion: v1
kind: Secret
metadata:
  name: joomla-db-secret
  labels:
    app.kubernetes.io/name: joomla
    app.kubernetes.io/component: database
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "${PARAM_JOOMLA_DB_ROOT_PASSWORD}"
  MYSQL_DATABASE: "joomla"
  MYSQL_USER: "joomla"
  MYSQL_PASSWORD: "${PARAM_JOOMLA_DB_PASSWORD}"
