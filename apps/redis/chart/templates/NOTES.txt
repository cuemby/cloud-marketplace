Cuemby Cloud Marketplace — Redis deployed!

Connect to Redis:

  {{- if eq .Values.redis.master.service.type "NodePort" }}
  redis-cli -h <VM_IP> -p {{ .Values.redis.master.service.nodePorts.redis }} -a <password>
  {{- else }}
  kubectl get svc -n {{ .Release.Namespace }}
  {{- end }}

Check deployment status:
  cat /var/lib/cuemby/marketplace-state.json
