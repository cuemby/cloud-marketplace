# Cuemby defaults for Redis on single-node K3s.
# These override the upstream bitnami/redis defaults for our environment.

redis:
  # Single-node architecture (no replicas on K3s)
  architecture: standalone

  # Authentication (password set via PARAM_REDIS_PASSWORD)
  auth:
    enabled: true

  # Master node configuration
  master:
    # Expose via NodePort (no ingress controller on K3s with Traefik disabled)
    service:
      type: NodePort
      nodePorts:
        redis: "30379"

    # Conservative resource requests for shared VMs
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 512Mi

    # Use K3s default StorageClass
    persistence:
      enabled: true
      storageClass: local-path
      size: 8Gi

    # Tuned probes for slower VM startup
    startupProbe:
      enabled: true
      initialDelaySeconds: 20
      periodSeconds: 5
      timeoutSeconds: 5
      failureThreshold: 24
      successThreshold: 1
    livenessProbe:
      initialDelaySeconds: 30
      periodSeconds: 10
      timeoutSeconds: 5
      failureThreshold: 6
    readinessProbe:
      initialDelaySeconds: 10
      periodSeconds: 5
      timeoutSeconds: 3
      failureThreshold: 6

  # Disable replicas (standalone architecture)
  replica:
    replicaCount: 0

  # Disable metrics by default
  metrics:
    enabled: false
