Cuemby Cloud Marketplace — Keycloak deployed!

Access Keycloak:

  {{- if eq .Values.keycloak.service.type "NodePort" }}
  Admin Console: http://<VM_IP>:{{ .Values.keycloak.service.nodePorts.http }}
  {{- else }}
  kubectl get svc -n {{ .Release.Namespace }}
  {{- end }}

Check deployment status:
  cat /var/lib/cuemby/marketplace-state.json
