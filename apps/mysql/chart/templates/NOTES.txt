Cuemby Cloud Marketplace — MySQL deployed!

Connect to MySQL:

  {{- if eq .Values.mysql.primary.service.type "NodePort" }}
  mysql -h <VM_IP> -P {{ .Values.mysql.primary.service.nodePorts.mysql }} -u root -p
  {{- else }}
  kubectl get svc -n {{ .Release.Namespace }}
  {{- end }}

Check deployment status:
  cat /var/lib/cuemby/marketplace-state.json
