apiVersion: v1
kind: Service
metadata:
  name: openclaw-http
  labels:
    app.kubernetes.io/name: openclaw
    app.kubernetes.io/component: gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: openclaw
    app.kubernetes.io/component: gateway
  ports:
    - name: http
      port: 80
      targetPort: 18789
      protocol: TCP
