apiVersion: v1
kind: Service
metadata:
  name: selenium-node-chrome
  labels:
    app.kubernetes.io/name: selenium
    app.kubernetes.io/component: node-chrome
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: selenium
    app.kubernetes.io/component: node-chrome
  ports:
    - name: node
      port: 5555
      targetPort: 5555
      protocol: TCP
