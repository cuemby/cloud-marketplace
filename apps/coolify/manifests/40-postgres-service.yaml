apiVersion: v1
kind: Service
metadata:
  name: coolify-postgres
  labels:
    app.kubernetes.io/name: coolify
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: coolify
    app.kubernetes.io/component: database
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
