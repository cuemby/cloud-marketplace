# Minimal profile for small VMs (2 CPU / 4 GB RAM).

influxdb:
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 1Gi
  persistence:
    size: 8Gi
