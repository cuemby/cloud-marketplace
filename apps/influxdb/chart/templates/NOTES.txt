Cuemby Cloud Marketplace — InfluxDB deployed!

Access InfluxDB:

  {{- if eq .Values.influxdb.service.type "NodePort" }}
  UI:  http://<VM_IP>:{{ .Values.influxdb.service.nodePorts.http }}
  API: curl -H "Authorization: Token <admin-token>" http://<VM_IP>:{{ .Values.influxdb.service.nodePorts.http }}/api/v2/buckets
  {{- else }}
  kubectl get svc -n {{ .Release.Namespace }}
  {{- end }}

Check deployment status:
  cat /var/lib/cuemby/marketplace-state.json
