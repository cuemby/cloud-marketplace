# openclaw E2E fixture — provides a dummy Anthropic key so pre-install passes.
# The gateway starts but AI calls will fail (acceptable for deployment tests).
export PARAM_OPENCLAW_ANTHROPIC_API_KEY="sk-ant-e2e-test-dummy-key"
